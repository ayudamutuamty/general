import { SortDistancePipe } from './sort-distance.pipe';

describe('SortDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new SortDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
